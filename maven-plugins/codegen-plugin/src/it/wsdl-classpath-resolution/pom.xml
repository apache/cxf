<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

    <groupId>org.apache.cxf</groupId>
    <artifactId>cxf5052-codegen</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>cxf5052-codegen</name>

    <properties>
        <wsdl.groupid>org.apache.cxf.cxf5052</wsdl.groupid>
        <wsdl.artifactid>Cxf5052Service</wsdl.artifactid>
        <wsdl.version>1.0.0</wsdl.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-install-plugin</artifactId>
                <version>2.5.2</version>
                <executions>
                    <execution>
                        <phase>initialize</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <groupId>${wsdl.groupid}</groupId>
                            <artifactId>${wsdl.artifactid}</artifactId>
                            <version>${wsdl.version}</version>
                            <packaging>wsdl</packaging>
                            <file>Cxf5052Service-1.0.0.wsdl</file>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.cxf</groupId>
                <artifactId>cxf-codegen-plugin</artifactId>
                <version>3.2.3-SNAPSHOT</version>
                <executions>
                    <execution>
                        <id>generate-sources</id>
                        <phase>generate-sources</phase>
                        <configuration>
	                        <wsdlRoot>${basedir}</wsdlRoot> 
	                   		<includes> 
								<include>**/*.wsdl</include> 
							</includes>
							<defaultOptions>
								<extraargs>
									<extraarg>-impl</extraarg>
									<extraarg>-server</extraarg>
									<extraarg>-client</extraarg>
	                                <extraarg>-verbose</extraarg>
	                                <extraarg>-validate</extraarg>
								</extraargs>
								<wsdlLocation>classpath;${basedir}</wsdlLocation>
							</defaultOptions>
                        </configuration>
                        <goals>
                            <goal>wsdl2java</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
